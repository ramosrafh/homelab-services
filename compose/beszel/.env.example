# URL pública do hub — usada em links de notificações e geração de config de agentes
APP_URL=https://beszel.seu-dominio.com

# Token e chave do agente local (comunicação via unix socket)
# Obtidos no hub após o primeiro acesso: Add System > copiar TOKEN e KEY gerados
AGENT_TOKEN=
AGENT_KEY=
